{{ form_start(form) }}
    {% if user.avatarFileName %}
        <img src="{{ path }}/{{ user.avatarFileName }}" />
    {% endif %}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
